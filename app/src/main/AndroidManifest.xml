<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dirceubelem.exemplo7">

    <application
        android:name=".Exemplo7"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DadosCompra"
            android:label="@string/title_activity_dados_compra"></activity>
        <activity
            android:name=".ItemCompra"
            android:label="@string/title_activity_item_compra"></activity>
        <activity
            android:name=".ListaItens"
            android:label="@string/title_activity_lista_itens"></activity>
    </application>

</manifest>
